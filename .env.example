# Copy to .env or set in docker-compose environment (do not commit .env)
FINNHUB_API_KEY=your_finnhub_api_key_here
FLASK_SECRET_KEY=change-me

# Optional fallback for Telegram bot token (prefer Settings → Integrations in the app)
# TELEGRAM_BOT_TOKEN=
