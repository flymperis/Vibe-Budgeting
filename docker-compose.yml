services:
  budget-app:
    build: .
    container_name: budget-app
    ports:
      - "5000:5000"
    environment:
      # SQLite on a mounted folder survives container recreate (see README).
      - DATABASE_PATH=/app/data/database.db
    volumes:
      - ./budget-data:/app/data
    restart: unless-stopped
